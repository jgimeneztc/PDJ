pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'building'
                sh '''
                docker-compose up
                '''
            }
        }
    }
}
